import 'package:flutter/material.dart';

class AppColors {
  static const primaryBlue = Color(0xFF8EC5FC);

static const lightBlue = Colors.white;

static const cardBlue = Colors.white;

static const textDark = Color(0xFF2D4B68);

static const textGrey = Color(0xFF8A9AAD);

static const borderColor = Color(0xFFE8F0F8);
}